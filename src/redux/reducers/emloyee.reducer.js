let initialState={
   employee:null
}

const EmployeeReducer =(state=initialState,actions )=>{
   switch (actions.type) {
      default:
      return {...state}   
   }

}
export default EmployeeReducer